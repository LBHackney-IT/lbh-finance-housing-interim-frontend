import "./assets/backgroundColourContainer.scss";

const BackgroundColourContainer = ({ children }) => {
  return <div className="background-colour-container">{children}</div>;
};

export default BackgroundColourContainer;
